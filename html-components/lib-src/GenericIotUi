<script>
	var ui = angular.module("ui");
	ui.controller("GenericComponentCtrl", ['$scope', '$http', '$interval', '$location', function($scope, $http, $interval, $location) {
		descriptorsUrl = "getDescriptors"		
		$http.get($scope.getBoardUrl("getDescriptors")).success(function(response) {
			$scope.descriptors = response;
		})
	}]);
</script>
<div class="container-fluid" ng-controller="GenericComponentCtrl">
	<div class="row">
		<div class="col-md-4 col-md-offset-4">
			<div class="panel panel-primary text-center">
				<div class="panel-heading">Status</div>
				<div class="panel-body">
					<div ng-repeat="input in descriptors.inputs">
						<span> Value of {{input.label}} is {{status[input.property]}}</span>
					</div>
				</div>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-md-4 col-md-offset-4">
			<div class="panel panel-primary text-center">
				<div class="panel-heading">Commands</div>
				<div class="panel-body">
					<button ng-repeat="command in descriptors.commands" class="btn btn-block btn-lg" type="button" ng-click="invoke(command.command)">
						{{command.label}}
					</button>
				</div>
			</div>
		</div>
	</div>
	<div class="row">
		<div class=" col-md-8 col-md-offset-4"><span class="label label-default"><span class="glyphicon glyphicon-dashboard"></span> Free memory (SRAM): {{status.freeMem}} bytes</span>
		</div>
	</div>
// TODO REVIEW: asta tr sa stea in template. Orice aplicatie va avea acest lucru, indiferent de ce componente vor exista
</div>